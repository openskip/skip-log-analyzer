require 'spec_helper'

describe Warning do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Warning.create!(@valid_attributes)
  end
end
