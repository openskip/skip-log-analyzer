require 'spec_helper'

describe CacheHitLine do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    CacheHitLine.create!(@valid_attributes)
  end
end
