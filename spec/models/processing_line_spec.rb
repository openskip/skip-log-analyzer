require 'spec_helper'

describe ProcessingLine do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ProcessingLine.create!(@valid_attributes)
  end
end
