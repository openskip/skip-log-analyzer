require 'spec_helper'

describe ParametersLine do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ParametersLine.create!(@valid_attributes)
  end
end
