require 'spec_helper'

describe RenderedLine do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    RenderedLine.create!(@valid_attributes)
  end
end
